import { style } from '@vanilla-extract/css';

export const editVacancyForm = style({
    display: 'flex',
    flexDirection: 'column',
    gap: '2rem',
    width: '540px',
});

export const buttonContainer = style({
    display: 'flex',
    gap: '16px',
    paddingLeft: '170px',
});
